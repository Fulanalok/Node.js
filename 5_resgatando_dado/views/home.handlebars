<h1>Usuários cadastrados:</h1>
<div class="container">
  <div class="user-grid">
    {{#each users}}
      <div class="user-block">
        <h2>{{this.name}}</h2>
        <p>{{this.occupation}}</p>
        <div class="actions">
          <a href="/users/{{this.id}}">Ver detalhes</a>
        </div>
      </div>
    {{/each}}
  </div>
</div>